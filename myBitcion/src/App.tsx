
import RouterScreen from "./router/RouterScreen";

const App = () => {
		useEffect(() => {
		console.countReset(data)
	}, [data])
	return (
		<div className="font-sans">
			<RouterScreen />
		</div>
	);
};

export default App;
